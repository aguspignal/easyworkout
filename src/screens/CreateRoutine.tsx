import { StyleSheet, View } from "react-native"

export default function CreateRoutine() {
	return <View style={styles.container}>CreateRoutine</View>
}

const styles = StyleSheet.create({
	container: {
		flex: 1,
		justifyContent: "center",
	},
})
