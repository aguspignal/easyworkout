import { StyleSheet, View } from "react-native"

export default function ExercisesRepository() {
	return <View style={styles.container}>ExercisesRepository</View>
}

const styles = StyleSheet.create({
	container: {
		flex: 1,
		justifyContent: "center",
	},
})
