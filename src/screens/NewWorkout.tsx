import { StyleSheet, View } from "react-native"

export default function NewWorkout() {
	return <View style={styles.container}>NewWorkout</View>
}

const styles = StyleSheet.create({
	container: {
		flex: 1,
		justifyContent: "center",
	},
})
