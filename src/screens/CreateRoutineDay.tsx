import { StyleSheet, View } from "react-native"

export default function CreateRoutineDay() {
	return <View style={styles.container}>CreateRoutineDay</View>
}

const styles = StyleSheet.create({
	container: {
		flex: 1,
		justifyContent: "center",
	},
})
