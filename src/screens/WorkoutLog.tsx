import { StyleSheet, View } from "react-native"

export default function WorkoutLog() {
	return <View style={styles.container}>WorkoutLog</View>
}

const styles = StyleSheet.create({
	container: {
		flex: 1,
		justifyContent: "center",
	},
})
