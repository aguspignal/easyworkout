import { StyleSheet, View } from "react-native"

export default function Routine() {
	return <View style={styles.container}>Routine</View>
}

const styles = StyleSheet.create({
	container: {
		flex: 1,
		justifyContent: "center",
	},
})
